```删除
dw--删除一个单词(从单词开头开始删除)
de--删除一个字符串（从光标开始到单词结束）
d$--从当前光标开始删除到行末（包括最后一单词）
x --删除当前光标所在的字符
0 光标到行首
dd--删除一整行
3dd--往后删除三整行
------
2w--移到后边第二个单词的开头
3e--移到后边第三个单词的结尾
```



```替换
r +要替换的字符-- 将当前光标所在的字符替换
cw +替换的字符串 --从出错字符开始替换为输入字符	
```



